{
  "name": "@chat-room/shared",
  "version": "1.0.0",
  "description": "Shared types and utilities for chat room application",
  "private": true,
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rm -rf dist",
    "type-check": "tsc --noEmit",
    "lint": "eslint src --ext .ts",
    "lint:fix": "eslint src --ext .ts --fix"
  },
  "dependencies": {
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@chat-room/config": "workspace:*",
    "typescript": "^5.3.2"
  }
}
